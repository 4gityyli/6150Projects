

function cipher(string, number) {
    const alphabet = "abcdefghijklmnopqrstuvwxyz";
    let result = "";
    num = string.length;
    for (let i = 0; i <= num; i++) {

        alphabet[ + number]
    }

};

cipher("a")